import React, { FC } from "react";

const Header: FC = () => {
  return (
    <>
      <h2>Search for users by Nationality</h2>
      <h4>Select a checkbox to modify search</h4>
    </>
  );
};

export default Header;
